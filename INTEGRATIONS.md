# EnigmAgent integrations

The current maintained integrations are the ten execution-tested native factories in `platforms/python-sdk/enigmagent/adapters/`. See [the Python SDK](platforms/python-sdk/README.md), [runtime evidence](audit/integration-runtime-evidence.json), [integration delivery plan](docs/GATEWAY_V2_INTEGRATION_PLAN.md), and [platform status](platforms/README.md). Historical prototypes and independently published packages are not proof of current compatibility or upstream adoption.
