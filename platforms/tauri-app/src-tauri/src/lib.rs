// EnigmAgent Tauri library entry point.
// Re-exports main.rs logic for the lib crate target.
pub fn run() {
    // Delegate to the function defined in main.rs
    // (Tauri builder is defined in main.rs to keep things simple)
}
